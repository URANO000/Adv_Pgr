@model Usuario

@{
    Layout = "_LayoutExterno";
}

<!-- Section: Design Block -->
<section class="text-center text-lg-start">
    <!-- Jumbotron -->
    <div class="login-section">
        <div class="container login-container">
            <div class="row g-0 align-items-center">
                <!-- Imagen primero (lado izquierdo) -->
                <div class="col-lg-6 mb-5 mb-lg-0">
                    <img src="~/img/banner-img3.png" class="w-100 rounded-4 shadow-4" />
                </div>
                <!-- Tarjeta después (lado derecho) -->
                <div class="col-lg-5 mb-5 mb-lg-0">
                    <div class="card cascading-left bg-body-tertiary" style="backdrop-filter: blur(30px);">
                        <div class="card-body p-4 shadow-5 text-center">
                            <iconify-icon icon="mdi:paw" class="login-icon"></iconify-icon>
                            <h2 class="fw-bold mb-3">Cambiar Acceso</h2>

                            @if (ViewBag.Mensaje != null)
                            {
                                <div class="alert alert-danger text-center text-black">
                                    @ViewBag.Mensaje
                                </div>
                            }


                            <form id="formRecuperarAcceso" asp-controller="Home" asp-action="RecuperarAcceso" method="post">

                                <div class="form-floating mb-4">
                                    <input type="email" id="CorreoElectronico" name="CorreoElectronico" class="form-control" asp-for="CorreoElectronico"
                                           placeholder="Correo Electrónico" required />
                                    <label for="Contrasenna">CorreoElectronico</label>
                                </div>

                                <!-- Submit button -->
                                <button type="submit" class="btn btn-primary btn-lg w-100 mb-4">
                                    <iconify-icon icon="mdi:login" class="me-2"></iconify-icon>
                                    Ingresar
                                </button>

                                <div class="text-center">
                                    <p class="mb-0">
                                        ¿Ya tienes cuenta?
                                        <a asp-controller="Home" asp-action="Login" class="fw-bold text-decoration-none">
                                            Inicia sesión
                                        </a>
                                    </p>
                                </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

@section Scripts{
    <script src="~/js/recuperarAcceso.js" asp-append-version="true"></script>
}